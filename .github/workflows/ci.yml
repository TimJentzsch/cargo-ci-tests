# modified from: https://github.com/bevyengine/bevy/blob/main/.github/workflows/ci.yml
name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

env:
  CARGO_TERM_COLOR: always

jobs:
  cargo-udeps:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: nightly
          override: true
      - name: Cache Cargo build files
        uses: Leafwing-Studios/cargo-cache@e5178c61da68275b6883d271b60a8036a672eba0
      - name: Print debug info
        run: |
          echo "Cargo.toml hash: '${{ hashFiles('**/Cargo.toml') }}'"
          echo "Cargo.lock hash: '${{ hashFiles('**/Cargo.lock') }}'"
          echo "Files ./"
          ls
          echo "Files cargo"
          ls ~/.cargo
      - name: Install alsa and udev
        run: sudo apt-get update; sudo apt-get install --no-install-recommends libasound2-dev libudev-dev
      - name: Installs cargo-udeps
        run: cargo install cargo-udeps@0.1.35
        # Cargo gives an error if it's already installed
        continue-on-error: true
      - name: Cargo udeps 1
        env:
          CARGO_LOG: cargo::core::compiler::fingerprint=info
        run: cargo udeps
      - name: Print debug info 1
        run: |
          echo "Cargo.toml hash: '${{ hashFiles('**/Cargo.toml') }}'"
          echo "Cargo.lock hash: '${{ hashFiles('**/Cargo.lock') }}'"
          echo "Files ./"
          ls
          echo "Files cargo"
          ls ~/.cargo
      - name: Cargo build
        env:
          CARGO_LOG: cargo::core::compiler::fingerprint=info
        run: cargo build
      - name: Print debug info 2
        run: |
          echo "Cargo.toml hash: '${{ hashFiles('**/Cargo.toml') }}'"
          echo "Cargo.lock hash: '${{ hashFiles('**/Cargo.lock') }}'"
          echo "Files ./"
          ls
          echo "Files cargo"
          ls ~/.cargo
      - name: Cargo udeps 2
        env:
          CARGO_LOG: cargo::core::compiler::fingerprint=info
        run: cargo udeps
      - name: Print debug info 3
        run: |
          echo "Cargo.toml hash: '${{ hashFiles('**/Cargo.toml') }}'"
          echo "Cargo.lock hash: '${{ hashFiles('**/Cargo.lock') }}'"
          echo "Files ./"
          ls
          echo "Files cargo"
          ls ~/.cargo
